@model IEnumerable<Psu>

@{
    ViewBag.Title = "Psu";
    Layout = "_Layout";
}

<!DOCTYPE html>
<html>
<head>
    <title>Psu</title>
</head>
<body>
<div class="col-md-3">
    <form class="form-inline" asp-controller="Psu" asp-action="Search" style="padding-right: 5px;">
        <input name="search" type="text" class="form-control bg-dark text-white" placeholder="Search by name" aria-label="Recipient's username">
    </form>
</div>
<p></p>
<div class="row">
    <div class="col-md-12">
        <table class="table table-bordered table-sm">
            <thead class="table-dark">
            <tr class="text-center">
                <th>@Html.DisplayNameFor(x => x.PsuId)</th>
                <th>@Html.DisplayNameFor(x => x.PsuModel)</th>
                <th>@Html.DisplayNameFor(x => x.Power)</th>
                <th>@Html.DisplayNameFor(x => x.Cabling)</th>
                <th>@Html.DisplayNameFor(x => x.Rgb)</th>
                <th>@Html.DisplayNameFor(x => x.Price)</th>
                @if (User.Identity.IsAuthenticated && User.IsInRole("Admin"))
                {
                    <th>Actions</th>
                }
            </tr>
            </thead>
            <tbody>
            @foreach (var x in Model)
            {
                <tr class="text-center">
                    <td class="align-middle">
                        @Html.DisplayFor(modelItem => x.PsuId)
                    </td>
                    <td class="align-middle">
                        @Html.DisplayFor(modelItem => x.PsuModel)
                    </td>
                    <td class="align-middle">
                        @Html.DisplayFor(modelItem => x.Power)
                    </td>
                    <td class="align-middle">
                        @Html.DisplayFor(modelItem => x.Cabling)
                    </td>
                    <td class="align-middle">
                        @Html.DisplayFor(modelItem => x.Rgb)
                    </td>
                    <td class="align-middle">
                        @Html.DisplayFor(modelItem => x.Price)
                    </td>
                    @if (User.Identity.IsAuthenticated && User.IsInRole("Admin"))
                    {
                        <td class="align-middle">
                            <a class="btn btn-success" asp-action="Edit" asp-route-id="@x.PsuId">Edit</a>
                            <a class="btn btn-danger" asp-action="Delete" asp-route-id="@x.PsuId">Delete</a>
                        </td>
                    }
                </tr>
            }
            </tbody>
        </table>
    </div>
</div>
@if (User.Identity.IsAuthenticated && User.IsInRole("Admin"))
{
    <div class="text-white">
        <a asp-controller="Psu" asp-action="Create" class="btn btn btn-dark">Add Psu</a>
    </div>
}
</body>
</html>